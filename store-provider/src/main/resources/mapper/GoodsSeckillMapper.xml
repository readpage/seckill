<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.GoodsSeckillMapper">
    <select id="findById" parameterType="Integer" resultType="GoodsSeckill">
        SELECT * FROM goods_seckill WHERE id=#{id}
    </select>
    <select id="selectAll" resultType="GoodsSeckill">
        SELECT * FROM goods_seckill
    </select>
    <insert id="add" parameterType="GoodsSeckill">
        INSERT INTO goods_seckill(seckill_price, stock_count, start_date, end_date)
        VALUES(#{seckill_price}, #{stock_count}, #{start_date}, #{end_date})
    </insert>
    <delete id="deleteById" parameterType="Integer">
        DELETE FROM goods_seckill WHERE id=#{id}
    </delete>
    <update id="updateById" parameterType="GoodsSeckill">
        UPDATE goods_seckill SET seckill_price=#{seckill_price}, stock_count=#{stock_count},
            start_date=#{start_date}, end_date=#{end_date} WHERE id=#{id}
    </update>
</mapper>